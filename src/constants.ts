export const TERMINAL_TITLE = "PI Code";

export const BRIDGE_EXTENSION_PATH = "bridge/pi-vscode-bridge.js";

export const BRIDGE_BOOTSTRAP_LINES = [
  "You are running inside VS Code with a live read-only IDE bridge.",
  "Prefer VS Code bridge tools over manual guesses when you need editor state, selection, diagnostics, symbols, definitions, type definitions, implementations, declarations, hovers, references, or workspace symbol search.",
  "Use the bridge for IDE/LSP context only; it does not expose tools that modify editor buffers or run editor actions.",
];
